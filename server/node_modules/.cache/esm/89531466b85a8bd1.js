let Joi,bcrypt,CustomErrorHandler,Teacher,RefreshToken,JwtService,REF_SECRET;_b85‍.x([["default",()=>_b85‍.o]]);_b85‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_b85‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_b85‍.w("../../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_b85‍.w("../../models",[["Teacher",["Teacher"],function(v){Teacher=v}],["RefreshToken",["RefreshToken"],function(v){RefreshToken=v}]]);_b85‍.w("../../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);_b85‍.w("../../config",[["REF_SECRET",["REF_SECRET"],function(v){REF_SECRET=v}]]);






const tloginController={
    async tlogin(req,res,next){
        //validation
        const tloginSchema = Joi.object({
            email: Joi.string().email().required(),
            password: Joi.string().pattern(new RegExp('^[a-zA-Z0-9]{3,30}$')).required(),
        });

        const {error} = tloginSchema.validate(req.body);

        if(error)
        {
            return next(error);
        }

        try{
            const teacher = await Teacher.findOne({email:req.body.email});

            if(!teacher){
                return next(CustomErrorHandler.wrongCredentials());
            }

            //compare password with database
            const match = await bcrypt.compare(req.body.password,teacher.password);
            if(!match){
                return next(CustomErrorHandler.wrongCredentials()); 
            }

            //token generate
            const access_token = JwtService.sign({_id:teacher._id,role:teacher.role});
            const refresh_token = JwtService.sign({_id:teacher._id,role:teacher.role},'1y',REF_SECRET);

            //Refresh token Send to database
            await RefreshToken.create({token: refresh_token});


            res.json({access_token:access_token,refresh_token:refresh_token})

        }catch(err){
            return next(err);
        }

    },
    async logout(req,res,next){
        //validation
        const refreshSchema = Joi.object({
            refresh_token: Joi.string().required(),
            
        });

        const {error} = refreshSchema.validate(req.body);

        if(error)
        {
            return next(error);
        }

        try{
            await RefreshToken.deleteOne({token:req.body.refresh_token});
        }catch(err){
            return next(new Error('Something went wrong in the database'));
        }
        res.json({status:1}); 
    }
};


_b85‍.d(tloginController);